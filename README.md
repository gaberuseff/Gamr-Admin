# 🎮 Gamr Admin Dashboard

لوحة تحكم احترافية لإدارة متجر العطور Gamr، مبنية باستخدام React و Supabase مع واجهة مستخدم عصرية وميزات متقدمة.

## 📋 نظرة عامة

Gamr Admin Dashboard هو نظام إدارة محتوى (CMS) متكامل مصمم خصيصاً لإدارة متجر العطور الإلكتروني. يوفر النظام واجهة سهلة الاستخدام لإدارة المنتجات، الطلبات، المستخدمين، والإعدادات مع لوحة معلومات تحليلية شاملة.

## ✨ الميزات الرئيسية

### 🏠 لوحة المعلومات (Dashboard)
- **إحصائيات في الوقت الفعلي**: عرض إجمالي الطلبات، الإيرادات، المنتجات، والمستخدمين
- **مخطط المنتجات الأكثر مبيعاً**: رسم بياني تفاعلي يعرض أفضل المنتجات مبيعاً مع إمكانية تخصيص العدد (1-10 منتجات)
- **مخطط حالة الطلبات**: رسم دائري (Pie Chart) يوضح توزيع الطلبات حسب الحالة (مستلم، مؤكد، تم الشحن، ملغي)
- **فلترة حسب الفترة الزمنية**: إمكانية عرض البيانات لآخر 7، 30، أو 90 يوم
- **رسائل الحالة الفارغة**: عرض رسائل واضحة عند عدم وجود بيانات

### 📦 إدارة المنتجات (Products)
- **عرض المنتجات**: جدول تفاعلي مع ترقيم الصفحات (6 منتجات لكل صفحة)
- **إضافة منتج جديد**: نموذج شامل يتضمن:
  - الاسم بالعربية والإنجليزية
  - الوصف بالعربية والإنجليزية
  - الأحجام المتاحة مع الأسعار
  - رفع صور متعددة للمنتج
  - حالة التوفر (متوفر/غير متوفر)
- **تعديل المنتجات**: تحديث بيانات المنتج مع إمكانية حذف/إضافة الصور
- **حذف المنتجات**: حذف آمن مع رسالة تأكيد
- **تصدير البيانات**: تصدير قائمة المنتجات إلى ملف Excel

### 🛒 إدارة الطلبات (Orders)
- **عرض الطلبات**: جدول شامل مع معلومات العميل والحالة
- **فلترة متقدمة**:
  - حسب حالة الطلب (الكل، مستلم، مؤكد، تم الشحن، ملغي)
  - البحث برقم الطلب أو رقم الهاتف
  - الترتيب حسب التاريخ أو السعر
- **تفاصيل الطلب**: صفحة مخصصة لكل طلب تعرض:
  - معلومات العميل الكاملة
  - المنتجات المطلوبة مع الأسعار
  - حالة الطلب مع إمكانية التحديث
  - الإجمالي والخصومات
- **ترقيم الصفحات**: 6 طلبات لكل صفحة
- **تصدير البيانات**: تصدير الطلبات لفترة محددة إلى Excel
- **إشعارات الطلبات الجديدة**: badge على زر الطلبات يعرض عدد الطلبات الجديدة

### 👥 إدارة المستخدمين (Users) - للمدير فقط
- **عرض جميع المستخدمين**: قائمة شاملة بالمستخدمين المسجلين
- **إضافة مستخدم جديد**: إنشاء حسابات للموظفين
- **إدارة الصلاحيات**: تحديد دور المستخدم (Admin/Employee)
- **حظر/إلغاء حظر**: التحكم في وصول المستخدمين
- **حذف المستخدمين**: إزالة الحسابات بشكل نهائي
- **الحماية**: صفحة محمية بصلاحيات Admin فقط

### ⚙️ الإعدادات (Settings)
- **إعدادات عامة**:
  - رقم الواتساب
  - رسوم الشحن
  - الحد الأدنى للطلب
  - رابط الموقع الإلكتروني
- **إدارة أكواد الخصم**:
  - إنشاء أكواد خصم جديدة
  - تحديد نوع الخصم (نسبة مئوية أو مبلغ ثابت)
  - تحديد تاريخ البداية والانتهاء
  - تفعيل/تعطيل الأكواد
  - تعديل وحذف الأكواد

### 👤 الملف الشخصي (Profile)
- **عرض المعلومات الشخصية**
- **تغيير كلمة المرور**
- **تحديث البيانات**

## 🛠️ التقنيات المستخدمة

### Frontend
- **React 19.2.0**: مكتبة JavaScript لبناء واجهات المستخدم
- **Vite 7.2.4**: أداة بناء سريعة وحديثة
- **React Router DOM 7.12.0**: للتنقل بين الصفحات
- **TanStack React Query 5.90.19**: لإدارة حالة البيانات والـ caching
- **Hero UI 2.8.7**: مكتبة مكونات UI حديثة
- **Tailwind CSS 4.1.18**: إطار عمل CSS utility-first
- **Recharts 3.7.0**: مكتبة الرسوم البيانية
- **React Hot Toast 2.6.0**: إشعارات أنيقة
- **React Icons 5.5.0**: مكتبة الأيقونات
- **XLSX 0.18.5**: لتصدير البيانات إلى Excel

### Backend & Database
- **Supabase**: منصة Backend-as-a-Service
  - **PostgreSQL Database**: قاعدة بيانات علائقية قوية
  - **Authentication**: نظام مصادقة متكامل
  - **Storage**: تخزين الصور والملفات
  - **Edge Functions**: دوال serverless للعمليات المعقدة
  - **Real-time Subscriptions**: تحديثات فورية للبيانات

### Development Tools
- **ESLint**: للحفاظ على جودة الكود
- **Supabase CLI**: لإدارة قاعدة البيانات والـ migrations

## 📊 هيكل قاعدة البيانات

### الجداول الرئيسية

#### `products`
```sql
- id (bigint, primary key)
- name_ar (text)
- name_en (text)
- description_ar (text)
- description_en (text)
- sizes (jsonb) - [{size, price}]
- images (text[])
- in_stock (boolean)
- created_at (timestamp)
```

#### `orders`
```sql
- id (text, primary key)
- customer_name (text)
- phone (text)
- address (text)
- city (text)
- products (jsonb)
- total (numeric)
- discount_code (text)
- discount_amount (numeric)
- status (text) - received/confirmed/shipped/cancelled
- created_at (timestamp)
```

#### `profiles`
```sql
- id (uuid, primary key)
- email (text)
- name (text)
- phone (text)
- role (text) - admin/employee
- is_blocked (boolean)
- created_at (timestamp)
```

#### `settings`
```sql
- id (bigint, primary key)
- whatsapp_number (text)
- shipping_cost (numeric)
- minimum_order (numeric)
- website_url (text)
```

#### `discount_codes`
```sql
- id (bigint, primary key)
- code (text, unique)
- discount_type (text) - percentage/fixed
- discount_value (numeric)
- start_date (date)
- end_date (date)
- is_active (boolean)
- created_at (timestamp)
```

### الدوال المخزنة (Stored Procedures)

#### `get_dashboard_stats(days_interval)`
تحسب إحصائيات لوحة المعلومات:
- إجمالي الطلبات
- إجمالي الإيرادات
- عدد المنتجات
- عدد المستخدمين

#### `get_order_status_distribution(days_interval)`
تحسب توزيع الطلبات حسب الحالة مع النسب المئوية

#### `top_selling_products(days_interval, limit_count)`
تعيد أكثر المنتجات مبيعاً خلال فترة محددة

### Edge Functions

#### `manage-user`
دالة Serverless لإدارة المستخدمين (حظر، إلغاء حظر، حذف):
- التحقق من صلاحيات Admin
- تحديث حالة المستخدم في Auth و Profiles
- معالجة الأخطاء بشكل آمن

## 🏗️ هيكل المشروع

```
src/
├── features/           # الميزات الرئيسية
│   ├── auth/          # المصادقة وتسجيل الدخول
│   ├── dashboard/     # لوحة المعلومات
│   ├── orders/        # إدارة الطلبات
│   ├── products/      # إدارة المنتجات
│   ├── settings/      # الإعدادات وأكواد الخصم
│   └── users/         # إدارة المستخدمين
├── pages/             # صفحات التطبيق
├── services/          # خدمات API
│   ├── apiAuth.js     # مصادقة المستخدمين
│   ├── apiDashboard.js # بيانات لوحة المعلومات
│   ├── apiOrders.js   # عمليات الطلبات
│   ├── apiProducts.js # عمليات المنتجات
│   ├── apiSettings.js # الإعدادات والخصومات
│   ├── apiUsers.js    # إدارة المستخدمين
│   ├── apiStorage.js  # رفع وحذف الصور
│   └── apiExport.js   # تصدير البيانات
├── ui/                # مكونات UI قابلة لإعادة الاستخدام
├── contexts/          # React Contexts
├── utils/             # دوال مساعدة
└── App.jsx           # المكون الرئيسي

supabase/
├── functions/         # Edge Functions
│   └── manage-user/   # إدارة المستخدمين
└── config.toml       # إعدادات Supabase
```

## 🔐 نظام الصلاحيات

### Admin
- الوصول الكامل لجميع الصفحات
- إدارة المستخدمين
- تعديل الإعدادات
- إدارة المنتجات والطلبات

### Employee
- عرض لوحة المعلومات
- إدارة المنتجات
- إدارة الطلبات
- عرض وتعديل الملف الشخصي
- **لا يمكن** الوصول لصفحة المستخدمين

## 🚀 البدء

### المتطلبات
- Node.js 18+
- npm أو yarn
- حساب Supabase

### التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd gamr-admin
```

2. **تثبيت الحزم**
```bash
npm install
```

3. **إعداد متغيرات البيئة**
أنشئ ملف `.env.local` في المجلد الرئيسي:
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_KEY=your_supabase_anon_key
```

4. **تشغيل التطبيق**
```bash
npm run dev
```

5. **بناء للإنتاج**
```bash
npm run build
```

## 🎨 الميزات المتقدمة

### 1. إدارة الذاكرة المؤقتة (Caching)
- استخدام React Query لتخزين البيانات مؤقتاً
- تحديثات تلقائية عند تغيير البيانات
- مسح الكاش الكامل عند تسجيل الخروج

### 2. التحديثات الفورية (Real-time)
- إشعارات فورية للطلبات الجديدة
- تحديث عداد الطلبات الجديدة تلقائياً
- استخدام Supabase Realtime Subscriptions

### 3. تحسين الأداء
- Lazy Loading للصفحات
- ترقيم الصفحات لتقليل حجم البيانات
- استرجاع ديناميكي لـ Access Token
- تحسين الصور قبل الرفع

### 4. تجربة المستخدم
- رسائل تأكيد قبل الحذف
- إشعارات نجاح/فشل واضحة
- رسائل حالة فارغة للمخططات
- تصميم متجاوب (Responsive)
- دعم اللغة العربية بالكامل

### 5. الأمان
- مصادقة آمنة باستخدام JWT
- حماية المسارات (Protected Routes)
- التحقق من الصلاحيات على مستوى الـ Backend

## 📱 الصفحات

### Dashboard (`/dashboard`)
لوحة معلومات تحليلية شاملة مع إحصائيات ورسوم بيانية

### Products (`/products`)
إدارة كاملة للمنتجات (عرض، إضافة، تعديل، حذف، تصدير)

### Orders (`/orders`)
إدارة الطلبات مع فلترة وبحث متقدم

### Order Details (`/orders/:orderId`)
عرض تفاصيل طلب محدد مع إمكانية تحديث الحالة

### Users (`/users`) - Admin Only
إدارة المستخدمين والصلاحيات

### Settings (`/settings`)
إعدادات المتجر وإدارة أكواد الخصم

### Profile (`/profile`)
الملف الشخصي للمستخدم الحالي

### Login (`/login`)
صفحة تسجيل الدخول

## 🔄 سير العمل

1. **تسجيل الدخول**: المستخدم يدخل البريد وكلمة المرور
2. **التحقق من الصلاحيات**: النظام يتحقق من دور المستخدم
3. **التوجيه**: يتم توجيه المستخدم للوحة المعلومات
4. **الوصول للميزات**: حسب الصلاحيات (Admin/Employee)
5. **العمليات**: إضافة/تعديل/حذف البيانات
6. **التحديثات الفورية**: تحديث الواجهة تلقائياً
7. **تسجيل الخروج**: مسح الكاش والتوجيه لصفحة الدخول

## 🤝 المساهمة

هذا مشروع خاص. للاستفسارات أو التحسينات، يرجى التواصل مع  gaberuseff.com.

---

**تم التطوير بواسطة**: gaberuseff.com